# 本仓库的 .mcp.json 不放明文密钥（用 ${ENV_VAR}），可安全提交，故不忽略。
# 若你要放本地私有覆盖配置，用下面这个并自行 gitignore：
.mcp.local.json

# gate-stop 定位当前 worktree 的运行态文件（每圈由 loop-implement 写、loop-persist 删），不入库
.claude/memory/current-worktree

# 每圈的隔离工作树放仓库内 .worktrees/（刻意不放同级 ../，避免 Claude Code 弹"访问工作区外目录"确认框）。运行态产物，不入库。
.worktrees/
